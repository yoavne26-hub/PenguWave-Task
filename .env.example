# Copy this file to `.env` and fill in real values.
# NEVER commit `.env` — it is gitignored. Secrets belong only in your local env.

# API key used by src/api.ts to talk to the events backend.
VITE_API_KEY=

# Optional: override the backend base URL (defaults to http://localhost:3001).
VITE_API_URL=
